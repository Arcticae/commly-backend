// eslint-disable-next-line no-shadow,import/prefer-default-export
export enum ConversationStateValue {
  FINISHED = 'finished',
  IN_PROGRESS = 'in-progress',
  PENDING = 'pending',
  FAILED = 'failed',
}
